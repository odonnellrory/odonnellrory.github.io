# 2026/02/22 - Landing + About + Style Unification

Today I reworked the homepage into a real landing page using HTML blocks in `docs/index.md`.

I wanted the page to feel like a front door for the whole site instead of just another markdown page.

Top section now has the big title, then a boxed area with the controls.

---

## What I changed

Added a proper landing layout with:

- section cards for Linux, Security, Archive, and Tic Tac Toe
- top bar controls
- social links
- latest journal button
- search bar

Search uses MkDocs search by sending queries to `search/` with `q=...`.

Also fixed the latest journal link logic so it points to the newest dated journal path and not a broken `.md` URL.

---

## About Me page

Built a dedicated About Me page in `docs/about.md` and filled out the full content.

Included:

- role and apprenticeship summary
- practical support and homelab focus
- project context
- featured repositories
- direct profile links (GitHub + LinkedIn)

---

## Styling pass across MkDocs pages

I styled the standard MkDocs content pages to match the landing/about visual language.

Changes included:

- shared gradient atmosphere + glass panel treatment
- matching typography and color system
- consistent sidebar/header/search treatment
- docs pages now use the same design direction as the HTML landing sections

---

## UI polish fixes

After rollout, I fixed two issues:

- sidebar section titles (`Archive`, `Table of contents`) visually overlapping nearby links
- background seam appearing while scrolling on long pages

Also removed the entire bottom footer area, not just the generator text.

---

## Directory rename

I renamed the docs directories and updated references so routing still works:

- `00-journal` -> `00-blog`
- `02-hack-the-box` -> `02-cybersecurity`
- `03-fedora` -> `01-docs`

Updated nav and links to match this, including homepage cards and internal markdown references.

---

## Result

Site now feels consistent end-to-end, with the landing page and MkDocs pages sharing one style system.

Homepage acts like a real front door with quick access to:

- latest journal
- search
- socials
- main sections

About page is now complete, and the docs area visually aligns with the rest of the site.
